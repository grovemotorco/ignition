/** A recipe file with only description in meta (no tags). */
export const meta = {
  description: "Setup base packages",
}

export default async function (_ctx: unknown): Promise<void> {
  // no-op
}
